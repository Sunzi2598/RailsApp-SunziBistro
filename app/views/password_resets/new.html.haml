- provide(:title, "Forgot password")

%h1.forgot Forgot password

.row
  %img.logo-image-password-reset{:alt => 'Sunzi Bistro logo', :src => '/assets/logo.jpg'}/
  
  .col-md-6.col-md-offset-3
    = form_with(url: password_resets_path, scope: :password_reset, local: true) do |f|

      = f.label :email
      = f.email_field :email, class: 'session form-control'

      = f.submit "Submit", class: 'session btn btn-primary'
      